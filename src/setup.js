#!/usr/bin/env node
"use strict";

const {
  add,       ifElse,
  always,    invoker,
  compose,   map,
  concat,    not,
  cond,      reduce,
  filter,    tap,
  flip,      test,
  identity,  T,
} = require("ramda");


module.exports =
  identity('ready');
