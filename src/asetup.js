const { identity } = require("ramda");

module.exports = identity(`
┌────────────────────────────────┐
│           - ready -            │
│  update src/buffer with a new  │
│      file for new output       │
└────────────────────────────────┘
`);
